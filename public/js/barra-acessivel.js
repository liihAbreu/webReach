(function($){
    var fontes = ['font-1x','font-2x','font-3x','font-4x'];

    $('#fonte-maior').click(function(){
        for(var i= 0; i <= fontes.length; i++){
            var fonteAtual = fontes[0+1]; 
        }
        console.log(fonteAtual);
        
    });

})($)